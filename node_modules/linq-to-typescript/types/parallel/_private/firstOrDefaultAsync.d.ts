import { IParallelEnumerable } from "../../types";
export declare const firstOrDefaultAsync: <TSource>(source: IParallelEnumerable<TSource>, predicate: (x: TSource) => Promise<boolean>) => Promise<TSource | null>;
