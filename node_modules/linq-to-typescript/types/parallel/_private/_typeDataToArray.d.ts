import { TypedData, LimitedTypedData } from "../../types";
/**
 * @private
 */
export declare const typeDataToArray: <TSource>(dataFunc: TypedData<TSource> | LimitedTypedData<TSource>) => Promise<TSource[]>;
