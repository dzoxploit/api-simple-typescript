import { IAsyncEnumerable } from "../../types";
export declare const skipWhileAsync: <TSource>(source: Iterable<TSource>, predicate: (x: TSource, index: number) => Promise<boolean>) => IAsyncEnumerable<TSource>;
