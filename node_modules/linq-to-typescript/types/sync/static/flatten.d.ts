import { IEnumerable, IFlatten } from "../../types";
declare type FlattenFunc = {
    /**
     * Flattens an iterable
     * @param source Iterable to flatten
     * @param shallow When false - recurses the iterable types
     */
    <TSource>(source: IFlatten<TSource>, shallow?: false): IEnumerable<TSource>;
    /**
     * Flattens an iterable
     * @param source Iterable to flatten
     * @param shallow When false - recurses the iterable types
     */
    <TSource>(source: IFlatten<TSource>, shallow: true): IEnumerable<TSource | Iterable<TSource>>;
};
export declare const flatten: FlattenFunc;
export {};
